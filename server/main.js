import { Meteor } from 'meteor/meteor';

// Eslint must be disabled here in order to Players db to work
// eslint-disable-next-line
import { Players } from '../imports/api/players';

Meteor.startup(() => {

});
